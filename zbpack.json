{
    "build_command": "composer install --no-dev --optimize-autoloader && npm ci && npm run build",
    "start_command": "php artisan storage:link --force && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
}
